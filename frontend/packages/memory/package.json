{
  "name": "@almamesh/memory",
  "version": "0.0.1",
  "license": "MIT",
  "description": "In-browser, zero-egress semantic memory for AlmaMesh chat: local embeddings plus an OPFS-backed SQLite + sqlite-vector Worker. No network, offline-first.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:unit": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@edgeproc/browser": "github:hseshadr/edgeproc-browser#333cbafd82856f4b662da2758f47b7afcf241969",
    "@huggingface/transformers": "^4.3.0"
  },
  "devDependencies": {
    "typescript": "~7.0.2",
    "vitest": "^5.0.1"
  }
}
