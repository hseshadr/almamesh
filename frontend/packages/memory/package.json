{
  "name": "@almamesh/memory",
  "version": "0.0.1",
  "license": "MIT",
  "description": "In-browser, zero-egress semantic memory for AlmaMesh chat: local embeddings plus an OPFS-backed SQLite + sqlite-vector Worker. No network, offline-first.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:unit": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@edgeproc/browser": "github:hseshadr/edgeproc-browser#a94e7f2a0237a7144658351c07cb296fcb0540fb",
    "@huggingface/transformers": "^3.8.1"
  },
  "devDependencies": {
    "typescript": "~5.7.3",
    "vitest": "^4.1.10"
  }
}
