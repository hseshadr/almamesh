/**
 * Shared chart components.
 *
 * The legacy `/astrologer-view` report surface was removed in favor of
 * `/report` (ReportView). `PlanetaryTable` survives because the dashboard's
 * `ChartVisualization` still renders it.
 */

export { PlanetaryTable } from './PlanetaryTable';
