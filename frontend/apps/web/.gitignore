# Large, git-ignored dev assets regenerated by scripts/setup-dev-assets.sh.
# (The pyodide/ + bundle/ + public.key rules live in the repo-root .gitignore;
# this is the per-package home for the self-hosted RAG model assets.)
#
# Self-hosted RAG embedding model (MiniLM q8 ONNX) + onnxruntime-web wasm.
# ~25 MB, fetched once from the HF Hub / copied from node_modules — never
# committed. Served same-origin at runtime (zero egress, offline).
public/models/

# Eye-inspection output of the @react-pdf report harness (`bun run report:pdf:sample`).
# A throwaway sample PDF for visual review — never committed.
.report-out/
